describe('C.DeviceActionは', function() {
    /* var deviceaction; */

    beforeEach(function() {
        // init
        /* deviceaction = new Global.DeviceAction(); */
    });
    afterEach(function() {
        // clear
    });

    it('DeviceMotion, DeviceOrientationクラスから使われる', function() {
        expect(0).to.be(0);
    });
});
